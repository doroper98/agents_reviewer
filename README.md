---
tier: 1
last_synced_with: v5.8.0
ssot_for:
  - "저장소 진입점 (50초 안에 무엇이고 어디로 가야 할지 알 수 있게 함)"
depends_on:
  - "src/orchestrator.py:VERSION"
  - "CHANGELOG.md"
  - "docs/ARCHITECTURE.md"
last_review: 2026-05-17
---

# Event Analysis Team — AI Agent System

텔레그램 메시지로 사건 분석을 지시하면, **2-call Tier 4 파이프라인** (ContextAnalyst Opus 4.7 → NarrativeComposer Opus 4.7) 이 보고서를 자유 형식으로 작성한 뒤 editorial 톤 HTML (LG 벤치마크 차용) 로 Cloudflare Pages 에 배포하는 시스템. v5.1.0 부터 매일 06:00 KST 자동 일일 브리핑 (`/briefing_on` 으로 구독). v5.2.0 부터 KRX / FRED / ECOS / Yahoo Finance 실 OHLC 시계열로 candle / line / area 금융 차트 자동 emit.

## Status
- Version: **v5.8.0** (SSOT: `src/orchestrator.py:VERSION`) — **후속 보고서에 '이 분석의 출발점' (원 보고서 제목 + 링크) 노출**: 후속(follow-up) 보고서 독자가 원 보고서의 사건·가정을 함께 보며 입체적으로 이해하도록, 원 보고서 연결 고리를 본문 서두 + 매리말 양쪽에 넣음. "부모 보고서" 내부 용어 대신 '이 분석의 출발점' 표현. 원 보고서 헤드라인을 `report_meta.report_title` 에 신규 저장(`ParentContext.parent_report_title` 필드), 제목 없으면 report_id 폴백. `freeform_essay.html` 에 `.freeform-origin` 박스(accent border-left, Newsreader serif 제목+링크) — 후속일 때만 노출. `report_meta` 에 `report_title` 컬럼 추가(구 DB idempotent 마이그레이션). 회귀 테스트 `TestReportMeta` 4종. 이전 v5.7.0 **일일 브리핑 날짜가 하루 어긋나던 회귀 fix (KST timezone SSOT)**: 5/31 06:00 KST 트리거 브리핑이 제목·본문에 "5/30" 을 박던 문제(스케줄러 안내·스코프 문구는 5/31 로 맞는데 보고서 내용만 전날) 해결. 원인은 UTC VM 에서 timezone 없는 `datetime.now()` 를 쓴 4곳(ContextAnalyst `current_date` / NarrativeComposer `publication_date` / bundle_builder `fetched_at`·`generated_at` / models `analysis_timestamp`) — 06:00 KST = 전날 21:00 UTC 라 전날로 셌다. `src/timeutil.py` 신설(`KST`/`now_kst()`/`today_kst()` SSOT, stdlib 고정 +09:00)로 통합, 사용자 노출 날짜는 항상 KST 경유. report_synthesizer 중복 KST 정의도 통합. 부수 효과로 ReportBundle `generated_at` 이 계약서가 요구하는 `+09:00` offset 으로 정렬. WRITE-AP-11 근본 원인(timezone) 보강. 배포된 봇은 fix 없어 VM 재배포 필요. 이전 v5.6.9 **미국 빅테크/반도체 개별주 + 미국 지수 차트 지원**: NVIDIA 주제 보고서인데 삼성·하이닉스·KOSPI 만 뜨던 문제(INSTRUMENT_REGISTRY 에 미국 개별주 부재) 해결. 미국 개별주 10종(NVDA·TSLA·AAPL·MSFT·GOOGL·AMZN·META·AMD·TSM·AVGO, Yahoo candle) + 미국 지수 3종(S&P500·나스닥·필라델피아 반도체, Yahoo line) 추가 — 레지스트리 11→24 종목. YahooFetcher 범용이라 코드 변경은 레지스트리+프롬프트+hook 뿐, 무인증. `_topic_priority_key` 로 주제(제목>요약) 등장 종목 우선 차트화 — 'NVIDIA 보고서엔 NVIDIA 차트' 보장. 이전 v5.6.8 **composer head-loss 회귀 픽스 (WRITE-AP-13)**: v5.6.7 부분 살림에도 또 짤린 보고서(analysis_20260530_130528, Duration 1397s) 진단 결과 — timeout 안 났고 정상 종료, raw 응답이 `\`\`\`json` 직후 `{` 가 아니라 `      "prose":` 처럼 sections 객체 *중간 줄* 부터 시작(head-loss). LLM 이 SYSTEM_PROMPT JSON 예시(6 spaces 들여쓰기)를 따라가다 응답 시작(`{`/headline/deck/sections 배열 시작) 통째로 누락. 2중 픽스 ① SYSTEM_PROMPT 에 ★★★ 강조 instruction 추가(`{` 로 시작 강제, 예시 중간 줄부터 시작 금지) ② `_recover_head_loss` 결정적 후처리 — body 가 `"key":` 패턴으로 시작하면 `{...}` 로 wrap 해 부분 sections 객체에서 prose/heading 추출 → 1-섹션 ComposedReport 재조립(confidence 0.3, summary "응답 시작 부분 누락"). 실제 회귀 case 6/6 검증. 이전 v5.6.7 **AI 인지 기호 박멸 (WRITE-AP-12, 사용자 최우선 규칙)**: 봇이 생성하는 모든 텍스트(보고서·SNS 문구)에서 마크다운 강조(`**`/`*`/백틱)와 em/en dash(`—`/`–`)를 절대 안 씀. 2중 방어 ① SYSTEM_PROMPT "★ 기호 금지" 블록 ② `NarrativeComposer._sanitize_symbols` 결정적 후처리(모든 노출 텍스트 정화, dash 자연 치환: 삽입구→쉼표·숫자범위→`~`·단어인접→공백, URL/좌표 보존) + orchestrator 최종 호출로 모든 경로(minimal fallback/hook/context 합성) 보장. `_clean_text` 13+`_sanitize_symbols` e2e 검증. 이전 v5.6.6 **composer 짤림 근본 수정 + SNS 문구 강건화**: deep 보고서가 timeout(540s)으로 두 번 다 죽어 부분 출력 폐기 → 0% "사실 자료만 표시" fallback 회귀(Duration 1306s = 540×2 산술 증거). 수정 4중 ① `_call_cli` stdout 임시파일화 → timeout kill 후에도 부분 출력 살림 ② `_repair_truncated_json`/`_drop_invalid_sections` 로 잘린 JSON 완성 섹션까지 복구 ③ timeout 후 재시도 안 함 ④ timeout 상향(deep 540→900·standard 360→480·fast 240→300s). 추가로 **SNS broadcast 결정적 폴백**(`_ensure_broadcast_summary`) — composer 누락/부분살림 시 deck+섹션 prose 로 평문 합성(마크다운 기호 배제), 텔레그램/일일브리핑이 SNS 문구를 항상 확보. JSON 복구 8케이스+broadcast 7케이스 오프라인 검증. 이전 v5.6.5 **두 버전 계보 통합**: `v5.5.5` 이후 분기된 배포 라인(main, `v5.5.11`)과 기능 라인(v5.6.x, `v5.6.4`)을 단일화. main 계보가 독자 추가한 기능(감시신호 수동버튼 v5.5.6 · 후속보고 deep 고정+chain depth 폐지 v5.5.7 · attribute access fix v5.5.8 · 한국 장마감 자동 브리핑+시장 구조 해석가 페르소나 v5.5.9 · `/status` market brief+`/watchlist` 4096자 fix v5.5.10 · composer `max_tokens` 1.5배+headline/deck 평이화+figcaption 정렬 v5.5.11 · patch_report `--recompose`)을 빠짐없이 cherry-pick. v5.6.x 의 X 공유 broadcast 요약·composer 타임아웃/재시도/파싱강건은 보존 — composer 중단을 타임아웃·재시도·토큰증량 **3중 방어**. ⚠️ 배포된 봇은 두 기능(X 공유·composer 안정화)이 없는 main `v5.5.11` 이라 VM 재배포 필수. 이전 v5.6.4 발행일과 사건일이 다를 때 본문에 **시점 앵커** 명시 강제(WRITE-AP-11). `_build_unified_payload` 에 `publication_date` 주입 + SYSTEM_PROMPT `=== 시점 앵커링 ===` 섹션. '같은 시각' 금지 + 지속 상태는 발행일 현재 기준 프레이밍. 이전 v5.6.3 관리자 **비공개 목록 페이지** `admin-{token}.html` (env `ADMIN_INDEX_TOKEN` 설정 시 — 고정 난수 주소라 즐겨찾기 가능, 전체 목록+토큰 URL). `/reports` 가 이 주소 함께 안내. 이전 v5.6.2 공개 인덱스 **목록 비공개** (구독자 전용 — 발급 링크로만) + `/reports` (텔레그램, 관리자 전용) 가 토큰 URL 포함 전체 목록 회수 (`_is_authorized` 게이팅 추가). 난수 URL 가드 완성 — 운영 시 `.env ALLOWED_CHAT_IDS` 에 본인 chat_id 설정 필수. 이전 v5.6.1 X(트위터) 구독자용 **broadcast 요약** (composer 가 `ComposedReport.broadcast_summary` emit — 해요/습니다 혼합·문단당 2문장·라벨 없음·마지막은 매번 다른 표현의 보고서 안내, 텔레그램 완료 메시지에 라벨 없이 첨부) + 보고서 **URL 난수 토큰** (`analysis_{ts}_{10hex}` — 추측 불가 unlisted 링크, 구독자 가드). 주의: 난수 URL 은 인증이 아니며 공개 인덱스가 나열하면 무력화. 이전 v5.6.0 feature 브랜치 통합. 행위자 관계도 **인접행렬**(CHART-AP-25, radial hairball 폐기) + ReportBundle **B안 폴백 SVG prerender**(계약 §5, `asyncio.to_thread`) + **slope 라벨 충돌 fix**(CHART-AP-26) + **composer 타임아웃·재시도·파싱강건**("보고서 중간 끊김 / 사실 자료만 표시" 회귀 fix — `_call_cli` 타임아웃 + `compose_unified` 재시도 + `raw_decode` 로 JSON 뒤 "Extra data" 무시). patch_report 는 main 의 `--replace`/`--add-footnote` 채택(우리 `--replace-text` 폐기). 단계별 상세는 DEVLOG (v5.5.6~v5.5.10 항목). 이전 v5.5.5 일반 독자 우선을 *최우선 가치* 로 명문화. 전문 용어·영어 표현 (rate card / rate limit premium 등) 을 평이한 우리말로 바꾸고, 못 바꾸는 핵심 용어만 그 섹션 `ComposedSection.footnotes` 로 *문단 하단 주석* ("용어 풀이" 블록) 렌더 (WRITE-AP-10). composer SYSTEM_PROMPT 최상단 "★ 최우선 원칙" + REPORT_STYLE_GUIDE §0.1. 이전 v5.5.4 `/bundle` 명령의 `import json` 누락 크래시 fix. 이전 v5.5.3 ReportBundle 항상 emit + 텔레그램 자동 첨부. 모든 `/analyze` 가 보고서+md+영상용 `bundle.json`(osint_generator)을 함께 전송. `/bundle <report_id>` 로 기존 보고서 번들도 회수. 이전 v5.5.2 시간 흐름도 capstone. 감시 신호 직후에 과거(context.timeline)→현재→미래(watch_signals deadline / 시나리오 분기) 시간 척추를 렌더 (과거=실선, 미래=점선/예상). 하이브리드 — 결정론 backbone + composer 선택적 윤색(`timeline_flow`). ReportBundle 에도 additive 로 실림. 이전 v5.5.1 모순 섹션 서술형 전환 + 동적 제목 (WRITE-AP-9). 정적 "봉합하지 않은 충돌" H2 폐기 → composer 가 보고서마다 판단형 제목(`contradictions_heading`) emit, resolution 이 단락의 마지막 문장으로 흐르며 착지. 각주형 라벨/박스 제거. 이전 v5.5.0 osint_generator (영상 자동제작) 연동용 **ReportBundle 핸드오프** (`analysis_{ts}.bundle.json`) emit. 보고서의 차트/지도/signals/섹션 + 출처·검증 메타 (verification 라벨 척추) 를 버전 박힌 기계판독 JSON 으로 `/analyze --bundle` 또는 `/bundle <report_id>` 시 산출. Q5 verification 은 결정론 배선 (market 출처 → confirmed, 그 외 → inferred, composer 프롬프트 무변경). 계약 SSOT: `docs/CONTRACTS/report_bundle_v1.md`. 이전 v5.4.9 자동 후속 보고서 생성 기능 비활성화 (사용자 요청). `src/models.py:MAX_CHAIN_DEPTH` 2 → 0 — 모든 watch signal 발화가 `next_depth >= 0` 조건에 걸려 child 보고서 enqueue 스킵. 부수 효과로 orchestrator 의 SQLite Watchlist Registry 등록도 정지 (부모 보고서 depth=0 도 cap 통과), `/fire` 수동 발화 경로도 사실상 정지. HTML 보고서의 "감시 신호" 섹션은 composed_report.watch_signals 에서 직접 렌더하므로 시각 영향 X. 재활성화 시 `MAX_CHAIN_DEPTH = 2` 복원. 이전 v5.4.8 forecast 차트 y축 도메인 + 실측 ↔ 예측 선 단절 fix (CHART-AP-23 / CHART-AP-24). `drawForecast` 의 yMin/yMax 산정이 `?? fallback` 으로 forecast 가 있으면 actual 무시 → actual 의 값이 forecast 범위 밖이면 데이터 점이 차트 영역 밖에 박힘. actual + forecast 모든 값 (.y, .low, .mid, .high) 산입으로 픽스. 동시에 actual 마지막 점을 forecast bridge 의 첫 점 (low=mid=high=actual.y) 으로 prepend → cone 이 fork 시점에서 한 점, 미래로 fan 형태로 확장 + dashed mid 선이 actual 끝점에서 시작해 연속. 표준 fan chart 컨벤션 (Bloomberg / FT / Economist) 적용. 이전 v5.4.7 sankey 좌·우 라벨 잘림 + 중간 컬럼 라벨 stacking 충돌 fix (CHART-AP-21 / CHART-AP-22). `drawSankey` 의 `computeZones` margin 을 `{left:8,right:8}` → `{left:80,right:120}` 로 확장 — 첫 컬럼 "DS 매출"/"100.0" 라벨이 음수 좌표까지 뻗어 잘리던 회귀 해소 + 마지막 컬럼 우측의 ~170px 휑함 보정. 동시에 `MIN_NODE_PAD` 18 → 36 — 위 라벨 (font 11) + 값 라벨 (font 10) stacking 에 부족해 메모리/파운드리 사이 "65.0" ↔ "파운드리" 라벨 7px overlap 회귀 해소. 8-노드 케이스 tightH 238 → 308 (여전히 원래 H=320 보다 작음). 이전 v5.4.6 sankey 차트 "위로 쏠림" fix (CHART-AP-20). `drawSankey` 에 content-fit viewBox 패스 추가 — 노드 positioning 끝난 뒤 실제 vertical extent (라벨 padding 포함) 측정해 viewBox H 를 타이트하게 재계산 + 컨텐츠를 SVG 위쪽 14px breath 까지 시프트. 8-노드 DS 매출 케이스 viewBox 320 → 238 (다크 스테이지 361px → 263px), 위·아래 여백 7.86 / 9.88px 로 균형. v5.3.0 의 sankey 4원칙 (anchor 압축 / source-weighted ordering / 분기 V 분산 / column y-centering) 은 보존, 결과 viewBox 만 압축. 모크업: `samples/sankey_lean_fix_v5_4_6.html` (Before/After). 이전 v5.4.5 hero 사진 figure 의 위·아래 여백 비대칭 fix. 사진 위쪽은 `.freeform-figure.hero` margin 18→12, 사진 아래쪽은 `.freeform-hero:has(.freeform-figure.hero)` 셀렉터로 hero section padding-bottom 32→14 (모바일 24→12) — caption 직후에서 다음 섹션 (목차/본문) 까지의 과한 공백 해소. figcaption padding-top 10→8 로 사진-캡션 간격도 살짝 좁힘. 사진 없는 보고서는 영향 무 (기존 padding-bottom 32 유지). 이전 v5.4.4 `analysis-reports.pages.dev` 인덱스 (`_generate_index`) 를 editorial_cream 톤으로 전환. 다크 버건디 (#2B1A1A / #C9A84C / Noto Sans KR) → cream (`--bg #F2EBDB / --accent #B05A38`) + Newsreader 800 (제목 + 보고서 링크) + IBM Plex Sans KR (본문) + IBM Plex Mono (eyebrow / 날짜 / footer). 7테마 풀 (mono guide §3.1) 의 editorial_cream 토큰 동일. samples 다이어그램 페이지와 시각 통일. 이전 v5.4.3 재무 보고서 sankey/waterfall 누락 fix (결정 트리 step 0). 이전 v5.3.2 감시 신호 섹션 editorial epilogue 화 (별개 H2 + chart-card 톤 폐기, lede + row 기반 list). 감시 신호 섹션 editorial epilogue 화. 별개 H2 (kicker "앞으로 무엇을 볼까" + heading "감시 신호") 와 chart-card 톤 (border + shadow + "시사" takeaway 박스) 폐기 — essay 마지막 호흡과 단절되어 "표" 처럼 끼어들던 회귀 해소. lede 한 문단 ("본 분석의 가정이 다음 N개 지점에서 시험된다") + 좌측 deadline mono + 우측 signal serif / desc sans / `시사 —` italic + accent border-left (contradiction-prose 의 ct-resolve 와 같은 어휘) 구조. 모바일은 deadline 을 본문 위로 올려 한 컬럼. composed.watch_signals Pydantic 무변경, convert_watch_signals() → SQLite Watchlist Registry 등록 경로 무영향. 모크업: `samples/watch_signals_redesign_compare.html` (좌측 v5.3.1 production / 우측 본 안). 이전 v5.3.1 entry 애니메이션 커버리지 확장 (option C). v5.3.0 의 type-무관 post-process 가 fill-only path 와 width-static rect 를 skip 하면서 보고서에서 가장 자주 등장하는 *donut* 과 *가로 bar* 가 사실상 정적으로 보이던 결함 해소. `drawBar` rect 에 `data-anim="bar-grow"` + `data-final-w` 태깅 → `_animateBars` 가 width 0→final, stagger 40ms / duration 380ms 으로 좌→우 성장. `drawDonut` arc 에 `data-anim="donut-arc"` + `data-start/data-end` 태깅 + SVG 에 `data-donut-cx/cy/ir/r` keep → `_animateDonut` 가 arcGen 을 재구성해 `attrTween('d')` 로 startAngle 위치에서 펼침 (duration 680ms). 일반 path 분기에 *fill-only / stroked-fill* → opacity fade 360ms 분기 신설 (sankey flow / choropleth 국경 / stacked_area / forecast cone / area gradient 회복). 데드 코드였던 `data-orig-dasharray` 보존 (dual_line / forecast 의 점선이 솔리드로 둔갑하던 silent 회귀 fix). CHART-AP-18 가드 (≤700ms, prefers-reduced-motion, 1회 재생, IntersectionObserver fallback) 보존. 이전 v5.3.0 FT/Economist 스타일 신규 7종 차트 (scatter / stacked_area / lollipop / slope / small_multiples / waterfall / range_bar) + 5-Layer Usage Guarantee (캔들 회귀 차단 안전망). production 차트 13 → 20종. SOFT_FAIL_RULES 5 → 6 (`chart_type_monotony` 추가 — standard ≥3 차트에 distinct <2 면 DeskEditor 가 다양화 지시). usage telemetry (`src/visual/usage_log.py`) JSONL 영구 기록 — 누적 ≥10 보고서에서 0회 emit type 을 WARNING 으로 표면화 (`python -m src.visual.usage_log analyze`). 모크업: `samples/chart_animation_mockup.html` (entry 애니메이션 검토용, production 이식은 별도 PR). 이전 v5.2.13 compact strip 만 보이고 풀 카드 (캔들/라인/area) 가 누락되던 회귀 수정. v5.2.5 부터 `_composer_instruments` 가 strip row 도 covered 로 인정하면서 composer 가 풀 카드 0 emit 시 fallback (`_ensure_time_series_chart`) 까지 막혀버린 부작용. `_count_existing_ts_charts` 가 strip 을 제외하고, fallback 에 *풀 카드 ≥1 강제* 안전망 추가 (data 가장 풍부한 instrument 1개를 strip dedupe 우회). `_drop_invalid_charts` 가 candle/line/area 를 silent drop 한 케이스도 자동 보강. 비-시계열 차트 (donut/bar/gantt/network/bubble/heatmap/stacked/dual_line/forecast/choropleth) 는 composer 직접 결정 — fallback 없음 (의도). 이전 v5.2.12 모순·신호 섹션 디자인 리뉴얼 + v5.2.11 간트 day-precision + 가로 막대 라벨 포맷 통일 + v5.2.10 compact-strip sparkline 의 부드러운 곡선 (curveMonotoneX 베지에 평탄화) 을 curveLinear segment 로 교체 → 실제 가격 흐름 (변동성·반전점) 그대로 노출 + baseline dashed line + min/max 극값 dot + 짧은 기간 라벨 (`24H` / `1W` / `3M` / `1Y`) 노출. 이전 v5.2.9 본문 문체 SSOT 통합 + persona 채널 폐기 + dead persona 모듈 청소 + v5.2.8 compact-strip 의 break-out (`width: min(1100px, ...)` + `translateX(-50%)`) 제거 → 본문 폭(.container max 960px) conform + desktop/tablet 공통 2-col grid 고정. 이전 v5.2.7 시계열 차트 takeaway 회귀 수정 + v5.2.6 DXY 라우팅 Yahoo/DX-Y.NYB 교체 + v5.2.5 compact-strip 회생 (key_figures inline 한 줄 sparkline strip) + grid overflow root-fix + 셀 사이 세로 separator + 모바일 stack 명시 설계 + v5.2.4 standalone 모드 (`report_synthesizer` 정적 자산 인라이닝) + v5.2.3 KOSPI 차트 4건 결함 패치 + v5.2.x Market Data Fetcher (KRX 개별주 via pykrx + KOSPI/KOSDAQ 지수 via Yahoo Finance + FRED 미국 매크로 + ECOS 한국 매크로) + 시계열 차트 type 2종 (candle / area) + chart_gate production wiring + mode-aware market period 상태 유지. 환경변수 `FRED_API_KEY` / `ECOS_API_KEY` 추가 시 다음 보고서부터 실 OHLC 차트 자동 emit. 활성화 가이드: [docs/V5_ACTIVATION.md](docs/V5_ACTIVATION.md).
- **V5 활성화 단계** — 코드는 v5.0.0 이지만 단계적 활성화 권장 (Plan §0.3). `docs/V5_ACTIVATION.md` 의 5-step 절차 따라 한 phase 씩 켜고 회귀 테스트 통과 확인.
- Tier 1 docs: [GOAL](GOAL.md) · [CLAUDE](CLAUDE.md) · [STYLEGUIDE](docs/STYLEGUIDE.md) · [DOCS_GOVERNANCE_V3](DOCS_GOVERNANCE_V3.md) · [MONO_THEME_GUIDE](docs/MONO_THEME_GUIDE.md)
- Tier 2 docs: [ARCHITECTURE](docs/ARCHITECTURE.md) · [DATA_MODELS](docs/DATA_MODELS.md) · [CATALOGS](docs/CATALOGS.md) · [TESTING](docs/TESTING.md)
- Tier 3 docs: [WORKFLOWS](WORKFLOWS.md) · [DEVLOG](DEVLOG.md) · [CHANGELOG](CHANGELOG.md)
- V5: [REFACTOR_V5_PLAN.md](REFACTOR_V5_PLAN.md) — V5 마스터 플랜 (proposal). v3 시대 SSOT 는 [docs/legacy/](docs/legacy/) 로 이전됨.

## Quick Start
```bash
git clone https://github.com/doroper98/agents_reviewer.git
cd agents_reviewer && python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt
git config core.hooksPath .githooks  # commit-msg hook 활성화 (Execution Rule #12)
cp .env.example .env  # 환경변수 입력
python -m src.main
```

## What This Does (v4.5.7 Tier 4)
- 텔레그램 봇이 사건 한 줄 메시지를 받음. `짧게/간략/요약` → fast, `심층/자세히/면밀` → deep, 그 외 → standard 모드 자동 결정.
- **2-call 파이프라인**: ContextAnalyst (Opus 4.7, 웹 검색, deep 모드 max_tokens 10K) 가 사실/타임라인/출처 수집 → NarrativeComposer (Opus 4.7, mode 별 max_tokens 12K/20K/32K) 가 *단일 호출* 로 행위자/구조/시나리오/모순 분석 + 보고서 본문 작성 + 감시 신호 추출 + 차트 데이터 + 지도 데이터까지 모두 emit.
- mode 는 composer 프롬프트의 분석 깊이 지시 (fast 3~4 섹션 / standard 4~6 / deep 5~7 + 모순 명시 필수) 와 max_tokens 한도에 영향. LLM 호출 수는 모든 모드 2회 동일.
- 보고서: `freeform_essay.html` 단일 템플릿. composer 의 `ComposedReport` (headline / sections / charts / map / watch_signals / contradictions / confidence) 를 mono 테마 (editorial_cream 디폴트 / burgundy_mono 위기·분쟁 한정) 로 렌더 → Cloudflare Pages 배포. 보고서 상단 hero 영역에 `system_version + revision` 추적 정보 표시.
- 차트 8종 (bar/donut/line/gantt/network/stacked/bubble/heatmap) 은 composer 가 데이터까지 직접 emit, charts.js 가 mono guide §4 패턴으로 SVG 렌더. 지도는 d3 + d3-geo + TopoJSON (maplibre 폐기). 무관한 지리 annotation (Somaliland 등) 은 viewport 교집합 검사 후 gate (CHART-AP-14).
- 감시 신호는 SQLite Watchlist Registry 에 등록 → `/watchlist`, `/fire` 명령으로 후속 추적.

> v3.x 의 7-agent + 11-lens + 11-archetype + 5-게이트 멀티 파이프라인은 **v4.0.0 부터 호출되지 않음**. 코드는 보존 (향후 cleanup commit 에서 제거 예정).

자세한 시스템 흐름은 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md), 에이전트·렌즈·archetype 카탈로그는 [docs/CATALOGS.md](docs/CATALOGS.md).

## Recent Changes
최신 9건 — 전체 [CHANGELOG.md](CHANGELOG.md):
- **v5.1.2** Daily Briefing 기본 트리거 시각 07:30 → 06:00 KST 조정. 시장 개장·외교 일정 전 조기 노출. env `DAILY_BRIEFING_TIME` override 그대로 유효, 기능/구조 변경 없음.
- **v5.1.1** orchestrator VERSION 상수 v5.1.1 로 bump (PR #22 후속). 감시 신호 발화 시 후속 보고서 자동 생성 + 텔레그램 알림 슬림화 + 출처 footnote 화.
- **v5.1.0** Daily Briefing Scheduler — 매일 지정 시각 (v5.1.0 기본 07:30 KST, v5.1.2 부터 06:00) "간밤 산업·지정학·정치·전쟁" 심층 보고서 자동 생성·배포·텔레그램 송신. `/briefing_on /briefing_off /briefing_status` 명령. 봇 프로세스 안 asyncio task (별도 cron 불요), SQLite 영속 구독, run_date PK 중복 방지. `DAILY_BRIEFING_ENABLED/TIME/TZ` env vars.
- **v5.0.0** V5 Refactoring 17-Phase 마스터 플랜 완료. 4-Tier 모두 인수. v4.5.7 byte-equal 보존, opt-in `V5_*` env flag.
- **v4.5.7** ContextAnalyst max_tokens deep 모드 4K → 10K. Somaliland (de facto) 폴리곤·legend viewport 교집합 검사 후 gate. CHART-AP-14 신설.
- **v4.5.6** hero eyebrow `Analysis Team v4.5.5 · Rev 0` 형식. revision 0 도 항상 표기.
- **v4.5.5** 보고서 상단에 `system_version + revision` 추적성 노출 (`FullAnalysisResult.system_version`, `revision`). patch_report.py mutate 시 revision +1.
- **v4.5.4** drawGantt 시간축 자동 추가 + note placement 막대 폭 분기 (CHART-AP-13). composer max_tokens mode 별 분기 (fast 12K / standard 20K / deep 32K). WRITE-AP-8 (max_tokens 절단) 신설.
- **v4.5.3** chart-card 배경 `var(--card)` 로 테마 귀속 + 각 테마 `--card-deep` 정의 (CHART-AP-11). drawBubble 스케일 `d3.extent` 자동 감지 (CHART-AP-12).
- **v4.5.2** fact-grid 항상 한 줄 (`data-cols` 강제). VERSION bump 누락분 (v4.5.0 → v4.5.2) 동기화.
- **v4.5.1** fact-grid 모바일 1 col stack — 홀수 타일 어색 wrap 차단 (v4.5.2 에서 정책 반전).
- **v4.5.0** Editorial 인터랙션 패턴 + Newsreader/IBM Plex 폰트 (LG 벤치마크 차용). 신규 디폴트 테마 `editorial_cream`, `burgundy_mono` 어둡게 + 위기/분쟁 한정. composer 평어체 (~다) + 신규 4 필드 (lede / analogy / fact_grid / dropcap) + 자동 TOC. WRITE-AP-7 (서수/식별번호 혼용) 추가.
- **v4.4.7** patch_report.py `--deck`/`--headline`/`--closing`/`--confidence-summary` 추가. WRITE-AP-1 회귀 fix (모든 raw text 필드에 `strip_md` filter 일괄 적용). WRITE-AP-7 신설.
- **v4.4.6** 지도 상단 배치 + d3.zoom() 인터랙션 + 소말릴란드 해칭 폴리곤. WRITE-AP-3 (지도 후행 배치) 회귀 fix.
- **v4.4.5** patch_report.py `--show` / `--map-zoom` / `--map-center` / `--remove-marker` + `--remove-chart` 다중 (인덱스 shift 자동 처리).
- **v4.2.0** Composer-emitted charts + maps. ComposedSection.charts (8 type) + ComposedReport.embedded_map 신설. charts.js 전면 재작성 (inline payload + mono guide §4 패턴). maps.js d3-geo + TopoJSON 으로 재작성, maplibre 폐기.
- **v4.1.0** ContextAnalyst → Opus 4.7. Tier 4 의 2-call 파이프라인에서 context 가 composer 가 보는 *유일한* 사실 입력이라 사실 추출 품질을 한 세대 위 모델로 상향. fast 모드 다운그레이드 제거.
- **v4.0.0** Tier 4 — UnifiedComposer 단일 호출 파이프라인 (MAJOR). 7개 분석 에이전트 + 11종 lens + 11종 archetype + 5단계 게이트 모두 호출 중단 (코드는 보존). LLM 호출 deep 13 → 2 (~85% ↓). archetype 매트릭스 라우팅 폐기, 항상 freeform_essay.
- **v3.5.0** Option C — narrative_composer 모든 모드에 활성. token_budget 통일. report_block.html 의 DATA DASHBOARD (9개 차트 슬롯 무지성 박힘) 통째 삭제. mono 테마 표준 적용 (lens_policy.select_theme 도 mono 만 emit).
- **v3.4.7** AMC 전체 archetype 적용 + required_inputs 검증 (PR4) — 12 archetype 에 contract() 적용. **pytest 202 passed**.
- **v3.4.6** AMC + Narrative DSL (PR3) — 단조로움의 *구조적* 처방. `NarrativeStage` Literal 5단계 (`fact / mechanism / divergence / decision / trigger`) + `ReportSectionPlan.narrative_stage`. `AnalysisMethodContract` Pydantic 모델 (mandatory_stages, forbidden_blocks, rationale). 5개 archetype 에 contract() 구현. 섹션 헤더 stage 배지 + 좌측 컬러 액센트로 시각 차별화.
- **v3.4.5** 시나리오 데이터 강화 (PR2) — `ScenarioAnalysis.scenarios[*]` 에 `confidence` (0~1 또는 0~100) + `driver_signals` (선행 지표 list) 필드 도입. `visual_builder.build_scenario_table` 정규화. `scenario_table.html` 카드 헤더에 신뢰도 배지 (색상이 신뢰도 따라 변화) + "선행 신호" 칩 list. backward-compat (loose dict).
- **v3.4.4** 보고서 품질 핫픽스 (PR1') — 4개 표면 문제 처리: ① `charts.js` TOKENS 하드코딩 → `getComputedStyle` (테마 동기), ② `visual_builder` Insight Gate (variance=0/value=1 차단, `_impact_magnitude()` 텍스트 키워드 추출), ③ `_payload_claim_card / evidence_table / qna` None 반환 (placeholder 회귀 차단), ④ `@media (max-width:540px)` 추가 (timeline 세로 스택, 테이블 카드 스택).
- **v3.4.3** 핫픽스 — `FullAnalysisResult.report_theme: str = ""` 필드 추가. v3.4.0 의 `_payload_map()` 이 `result.report_theme` 으로 light/burgundy 분기를 시도했지만 Pydantic 모델에 필드가 없어 `AttributeError` 로 보고서 생성 실패 → 분석 자체가 실패 판정. 한 줄 패치.
- **v3.4.2** `/stop` `/stopall` 명령 추가 — 진행 중 분석을 텔레그램에서 직접 중단. `/stop` 은 현재 1건만, `/stopall` 은 현재 + 대기열 전체 비움. asyncio.Task.cancel() 기반으로 LLM 호출/서브프로세스까지 시그널 전파. 인가 체크는 `/analyze` 와 동일.
- **v3.4.1** `/status` build info — 봇 프로세스 시작 시점에 git branch / short commit / commit date / dirty 여부를 한 번 캡처해 (`src/orchestrator.py:BUILD_INFO`) 시작 로그와 텔레그램 `/status` 응답에 노출. pull 만 하고 재기동을 안 한 경우에도 BUILD_INFO 는 *실행 중인 코드의* 커밋을 가리키므로 운영자가 버전 미스매치를 즉시 인지할 수 있다.
- **v3.4.0** `map` BlockType 추가 — maplibre-gl 4.7 + d3-geo v7 지도 블록을 보고서 파이프라인에 통합. `BlockType` Literal 에 `"map"` 추가 (총 18종), `_payload_map` 빌더 + `build_map_payload()` (visual_analyst 의 leaflet_config → MAP block payload 변환), `blocks/map.html` 템플릿, 정적 자산 `maps.js` / `maps.css`. light_mono / burgundy_mono 두 테마 + 골드 `#C9A84C` 단일 하이라이트. `geopolitical_strategic` archetype 의 "전장·행위자" 섹션에 자동 포함, 데이터 없으면 자동 스킵. **VM 재기동 필요.**
- **v3.3.1** Sample 추가 — `samples/theme_mono_map_chart.html` (maplibre-gl 4.7 + d3-geo v7). 라이트 모노 / 버건디 모노 두 팔레트에 동일 데이터셋 (동북아·동남아 항만 네트워크 + 16주 처리량) 을 입혀 `#C9A84C` 골드 단일 하이라이트 원칙을 보여주는 단일 페이지. 코드 영향 없음.
- **v3.3.0** Narrative Composer (Opus 4.7) — deep 모드 전용 freeform editorial pass. 정형 17 슬롯 대신 사건별 3~7 자유 섹션. 차트는 본문 흐름에 따라 composer 가 embed (auto-dashboard 폐지). claim 인용으로 evidence 추적성 보존. 새 archetype `freeform_essay` (총 12종). fast/standard 영향 0.
- **v3.2.0** d3 Chart Dashboard + Mobile-first Cards — d3 v7 인라인 임베드 (정적 자산), 9종 차트 라이브러리 (bar/donut/heatmap/triple/line/stacked/bubble/gantt/network), 시나리오 카드 그리드 (모바일 우선, 표 폐기), 보고서 자동 차트 생성 (데이터 가용성 기반), 차트 디자인 시스템 (charts.css), 차트 갤러리 샘플.
- **v3.1.0** Token Budget + Mode Routing — fast/standard/deep 모드 도입, Strategy Planner 프롬프트 축소 (~5x), AnalysisBrief compact context, deterministic visual/summary builder, 페르소나 deep-only, telemetry 도입. LLM 호출 ~50% 감소 (standard 기준).
- **v3.0.0** V3 Step 5-C — archetype 11종 완성 + 페르소나 → lens 이전 (StakeholderLens/StructuralLens/CascadeLens) + 하이브리드 라우팅. V3 리팩토링 완료.
- **v2.9.5** V3 Step 5-B — Watchlist Registry (SQLite, asyncio monitor, /watchlist /fire 명령, Anti-pattern #11)
- **v2.9.0** V3 Step 5-A — Lens Pool (8종) + archetype 3종 추가 (총 6) + 4-cap 가드 (Anti-pattern #6)
- **v2.8.0** V3 Step 4 — Quality Gate 1/2 + Claim-Evidence 추적성 + Synthesis Judge (모순 노출, 봉합 X)
- **v2.7.0** V3 Step 3 — 보고서 블록 렌더링 시스템 (17종 BlockType + report_block.html 디스패처)

## License
TBD
